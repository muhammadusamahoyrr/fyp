# Core
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
python-multipart==0.0.18
aiofiles==24.1.0
httpx==0.28.1

# Database
motor==3.6.0
pymongo==4.9.2
chromadb==1.5.9
pdfplumber==0.11.4
rank-bm25==0.2.2

# Auth & Security
email-validator==2.2.0
python-jose[cryptography]==3.3.0
cryptography==43.0.3
bcrypt==5.0.0
websockets==16.0

# Rate limiting
slowapi==0.1.9

# Redis (async client — WS fan-out, scheduler locks, caching/rate-limiting)
redis==5.2.1

# Email
aiosmtplib==3.0.1
jinja2==3.1.4

# Document generation
docxtpl==0.18.0
reportlab==4.2.5

# NLU intent pipeline (ONNX-based embeddings — no torch dependency)
fastembed==0.4.2

# AI / LangChain (pinned for reproducible installs)
langchain==0.3.30
langchain-core==0.3.86
langchain-community==0.3.27
langchain-groq==0.3.8
langchain-openai==0.3.35
langchain-chroma==1.1.0
langchain-huggingface==0.1.2
langgraph==1.0.1
nltk==3.9.4
# Optional LLM providers — install only if you point LLM_PROVIDER at them
# (llm.py imports them lazily and falls back if absent):
#   langchain-google-genai   # Gemini
#   langchain-ollama         # local Ollama

# Voice / STT
faster-whisper
soundfile

# Web search (no API key required)
duckduckgo-search==7.3.0

# Utilities
python-dotenv==1.0.1
arabic-reshaper==3.0.0
python-bidi==0.6.6
beautifulsoup4
pypdf
