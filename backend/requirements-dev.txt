# Dev / test tooling.  Install with:  pip install -r requirements-dev.txt
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.24.0

# In-memory Redis (incl. Streams + consumer groups) for the citator_stream tests.
# NOTE: fakeredis simulates XADD/XREADGROUP/XACK/XAUTOCLAIM semantics well enough
# for logic tests, but XAUTOCLAIM recovery *timing* (min-idle reclaim under real
# latency) should be spot-checked against a real Redis 7 before the demo.
fakeredis>=2.26

# Running the tests:
#   pytest                       # collects the assert-style tests (e.g. test_causelist_parser)
#   python tests/test_bail.py    # the self-contained tests also run directly
